import React, { useState, useEffect } from "react";
import io from "socket.io-client";
import "./Chat.css";

function Chat(props) {
  let squares = [none * 77].map((square, idx) => {
    <div id={idx}></div>;
  });
  return <h1>Chat</h1>;
}

export default Chat;
